Get all clone requests
---
tags:
  - Clone Request

parameters:
- name: token
  in: header
  description: API key
  required: false
  type: string
  format: string
- name: access-token
  in: header
  description: API key
  required: false
  type: string
  format: string 

responses:
    200:
      description: Get all clone requests successfully
      examples:
        result: "success"
        message: null
    404:
      description: exception in fetching clone requests
      examples:
        result: "failed"
        message: "Throws an exception"
