fetch all users
---
tags:
  - User
parameters:
- name: token
  in: header
  description: API key
  required: false
  type: string
  format: string
- name: access-token
  in: header
  description: API key
  required: false
  type: string
  format: string 

responses:
  200:
    description: All users fetched
    examples:
    result: "success"
    message: ""
  404:
    description: exception in fetching users
    examples:
    result: "failed"
    message: "Throws an exception"
